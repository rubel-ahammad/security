# Override from command line if you want:
# ./gradlew publish -PGROUP=com.ideascale.commons -PVERSION=0.1.0

GROUP=com.ideascale.commons
VERSION=0.1.0-SNAPSHOT

# Optional signing toggle:
# signingRequired=false
